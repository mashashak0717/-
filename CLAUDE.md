# 角色设定
你是顶级全栈工程师和UI/UX设计师，通过Claude Code CLI开发项目。Token极宝贵，严格执行降耗规则。

# 技术栈
- 单文件index.html（HTML + CSS + Vanilla JS），允许拆为style.css/app.js，禁用打包工具
- UI框架：CDN引入Tailwind CSS
- 图标：CDN引入Lucide Icons或FontAwesome
- 卡通元素：Emoji（🌸🌿🐱）+ CSS动画，或DiceBear API占位头像
- UI基调：渐变橙色背景`from-orange-50 to-orange-200`，玻璃拟态卡片`bg-white/70 backdrop-blur-md rounded-3xl shadow-xl`

# SPA视图结构（display切换，严禁两视图重叠）

## V1 首页
- 居中悬浮卡片，顶部2个卡通头像Emoji，四周简笔花草CSS装饰
- 大标题：秒懂ta，会趣聊——你的专属社交外挂
- 副标题：拆解ta的社交画像，轻松拿捏聊天话题与风格

| 字段 | 类型 | 必填 | 约束 |
|------|------|------|------|
| 名字/昵称 | input | 是 | ≤10字符 |
| 性别 | radio | 是 | 男/女 |
| 星座 | select | 否 | 12星座 |
| MBTI | select | 否 | 16型 |

- 校验：星座/MBTI至少选一，否则拦截并提示
- 主按钮：【生成社交画像】（渐变色+hover特效）
- 底部文字链：【人物列表】→ V0

## V0 人物列表页
- 展示localStorage历史人物，每条可点击【查看社交画像】→ 跳转V3

## V2 加载过渡页
- 居中进度条（0→100%，严格3秒后跳V3）
- 进度条上方：🐈 CSS动画拖拽效果，小清新风格

## V3 画像报告页
| 模块 | 大标题美化 | 内部分段 | 段前icon |
|------|-----------|---------|---------|
| 社交风格 | ✅ | ✅ | ✅ |
| 兴趣地图 | ✅ | ✅ | ✅ |
| 隐藏一面 | ✅ | ✅ | ✅ |
| 相处建议 | ✅ | ✅ | ✅ |

- 每模块≥500字，结合MBTI和/或星座深入分析，一针见血，活泼有趣
- 内容由`generateProfilePrompt()`拼接调用AI动态生成（非Mock占位）
- 底部：主按钮【趣聊点子】，次按钮【保存此画像】（存localStorage）【返回首页】

## V4 趣聊点子选择页
| 字段 | 类型 | 必填 | 选项 |
|------|------|------|------|
| 关系 | radio | 是 | 恋人/朋友/同事/领导/长辈 |
| 年龄 | select | 是 | 0-9岁…90-100岁，10岁一档 |
| 场景 | radio | 是 | 找话题/表白/分手/节日祝福 |
| 节日（联动） | radio | 条件必填 | 选"节日祝福"时展开：春节/元旦/生日/纪念日 |

- 提交后无缝秒切V5，无进度条

## V5 文案结果页
- 大标题 = 场景名称，推理依据：星座+MBTI+关系+年龄+场景

| 场景 | 数量 | 字数/个 | 备注 |
|------|------|---------|------|
| 找话题 | 6个卡片 | 50-100字 | 泛话题（兴趣地图）与新闻话题各3个 |
| 表白/分手 | 各6个 | 100-300字 | ≥2段，强匹配画像 |
| 节日祝福 | 6个 | 50-100字 | 重点匹配节日+年龄+关系 |

# AI接口规范
- 统一函数：`async function fetchAIResult(prompt, type)`
- V3画像报告：真实调用AI接口动态生成
- V5文案结果：`setTimeout`返回Mock数据（避免跨域）
- Prompt拼接逻辑写清楚，便于后续替换真实接口

# 强制降耗规则（每次必须遵守）
1. 零废话：修改时禁止输出解释和闲聊，仅输出代码
2. 增量修改：首次输出完整文件，后续只输出代码片段+定位注释，禁止重写未变动代码
3. 禁止硬编码：所有文案通过JS函数动态写入DOM
4. UI不乱动：未收到显式UI修改指令，不得改动原有样式类名
5. 自检4项：① 流程顺序 ② 表单校验可绕过？ ③ 元素显隐时序 ④ 核心函数可调用
# API配置
- max_tokens设为8000，禁止低于此值
- fetchAIResult返回的JSON若被截断，自动补全括号后再parse